<h2>投稿詳細</h2>

<p>タイトル</p>
<p><%= @list.title %></p>

<p>本文</p>
<p><%= @list.body %></p>

<p>画像</p>
<span>
 <%= image_tag @list.image , size: "200x200" %>
</span>

<%= link_to  "編集" , edit_list_path(@list.id) %>

<%= link_to '削除', list_path(@list.id), method: :delete, "data-confirm" => '本当に削除してもよろしいですか？' %>